# ================================
# 스마트팜 대시보드 환경 변수 설정
# ================================

# ── 서버 설정 ──────────────────────────────
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# ── Tuya Developer OpenAPI (투야 연동) ──────
TUYA_ACCESS_ID=qsdjvehhx7n8ptuth45v
TUYA_ACCESS_SECRET=f1b450e443494a30950e9ad0095e201f
TUYA_ENDPOINT=https://openapi.tuyaus.com
TUYA_PROJECT_CODE=p1786626333138paass9
TUYA_SAMPLE_DEVICE_ID=ebb219afdebea03ba3shlz

# ── MQTT 브로커 (HiveMQ Cloud - 커스텀 ESP32용) ─────
MQTT_HOST=your-cluster.hivemq.cloud
MQTT_PORT=8883
MQTT_USERNAME=your_mqtt_username
MQTT_PASSWORD=your_mqtt_password
MQTT_CLIENT_ID=smartfarm-backend-001

# ── InfluxDB (시계열 DB) ────────────────────
INFLUXDB_URL=http://influxdb:8086
INFLUXDB_TOKEN=my-super-secret-auth-token
INFLUXDB_ORG=smartfarm
INFLUXDB_BUCKET=greenhouse_telemetry

# ── PostgreSQL (관계형 DB) ──────────────────
DATABASE_URL=postgresql://smartfarm:smartfarm123@postgres:5432/smartfarm_db

# ── Redis (큐/캐시) ─────────────────────────
REDIS_URL=redis://redis:6379

# ── JWT 인증 ────────────────────────────────
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=24h

# ── CORS 설정 ───────────────────────────────
CORS_ORIGIN=http://localhost:5173

# ── 백엔드 모드 (tuya | mqtt | hybrid) ──────
DEVICE_PROVIDER=hybrid
